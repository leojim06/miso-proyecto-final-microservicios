export declare const configuration: () => {
    NODE_ENV: string;
    port: number;
    database: {
        host: string;
        port: number;
        dbName: string;
        user: string;
        password: string;
    };
};
